# 01. 统一总览

## 1. 作用

ChatBI 文档整理包只负责说明统一对话协议基线本身，不直接定义报告系统业务模型。

## 2. 覆盖范围

- 会话与单轮对话包络
- `ask / answer / steps` 结构
- SSE 事件模型
- 对外接口资源组织

## 3. 不覆盖内容

- 报告模板正式模型
- 模板实例正式模型
- Report DSL 正式模型
- 文档生成闭环

这些内容由 `design/report_system/` 负责。
