# 03. 运行时交互流程

## 1. 非流式交互

- 用户发起问题
- 服务返回 `ask` 或最终 `answer`

## 2. 流式交互

SSE 事件序列基线：

- `status`
- `step_delta`
- `ask`
- `answer`
- `error`
- `done`

## 3. 统一约束

- `ask` 和 `answer` 都必须放在统一事件包络中
- 流式增量不能绕开外层包络直接返回裸业务对象
- 最终完成时必须返回 `done`
