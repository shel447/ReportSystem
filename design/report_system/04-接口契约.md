# 04. 接口契约

## 1. 公开接口面

```text
POST   /rest/chatbi/v1/templates
GET    /rest/chatbi/v1/templates
GET    /rest/chatbi/v1/templates/{id}
PUT    /rest/chatbi/v1/templates/{id}
DELETE /rest/chatbi/v1/templates/{id}
POST   /rest/chatbi/v1/templates/import/preview
GET    /rest/chatbi/v1/templates/{id}/export

GET    /rest/chatbi/v1/chat
POST   /rest/chatbi/v1/chat
GET    /rest/chatbi/v1/chat/{conversationId}
DELETE /rest/chatbi/v1/chat/{conversationId}
POST   /rest/chatbi/v1/chat/forks

GET    /rest/chatbi/v1/reports/{reportId}
POST   /rest/chatbi/v1/reports/{reportId}/document-generations
GET    /rest/chatbi/v1/reports/{reportId}/documents/{documentId}/download

POST   /rest/chatbi/v1/parameter-options/resolve
```

统一身份头：

```http
X-User-Id: <external-user-id>
```

## 2. 模板接口

### 2.1 模板详情

模板接口直接返回正式模板对象：`ReportTemplate`

```json
{
  "id": "tpl_network_daily",
  "category": "network_operations",
  "name": "网络运行日报",
  "description": "面向网络运维中心的统一日报模板。",
  "schemaVersion": "template.v3",
  "parameters": [],
  "catalogs": []
}
```

规则：

- `POST /templates`：以 `ReportTemplate.id` 作为资源主键；若同 id 已存在，返回冲突错误，不做隐式覆盖
- `PUT /templates/{id}`：路径 `id` 与请求体 `ReportTemplate.id` 必须一致；不允许借更新接口改资源主键
- `GET /templates/{id}/export`：返回规范化后的正式 `ReportTemplate`，其结构应与模板详情接口等价
- 模板详情、创建、更新、导出都返回“规范化后”的正式模板对象，不返回原始导入文本

### 2.2 导入预览

请求：

```json
{
  "content": "...原始模板内容..."
}
```

规则：

- `content` 可为原始文本或对象；服务端负责规范化为正式 `ReportTemplate`
- 导入预览只做“解析、规范化、校验、告警”，不落库
- 导入预览不分配正式模板 id；若原始内容缺失必要 id，可在 `normalizedTemplate` 中给出建议 id，同时通过 `warnings` 明示

响应：

```json
{
  "normalizedTemplate": {},
  "warnings": [
    {
      "code": "TEMPLATE_FIELD_NORMALIZED",
      "message": "已将旧 sections 结构规范化为 catalogs -> sections。",
      "path": "$.catalogs[0]"
    }
  ]
}
```

规则：

- `normalizedTemplate` 必须已经满足正式模板 schema
- `warnings` 统一返回结构化对象：`code/message/path`
- `warnings` 只表达可自动规范化或可继续确认的问题；若无法形成合法模板，则应直接返回校验错误，而不是继续返回 `normalizedTemplate`

## 3. `/chat` 请求契约

### 3.1 ChatRequest

```json
{
  "conversationId": "conv_001",
  "chatId": "chat_001",
  "question": "帮我生成总部网络运行日报",
  "instruction": "generate_report",
  "reply": null,
  "attachments": [],
  "histories": [],
  "requestId": "req_001",
  "apiVersion": "v1"
}
```

关键字段：

- `conversationId`：会话 id
- `chatId`：当前轮 id
- `instruction`：当前正式支持 `generate_report`、`extract_report_template`
- `reply`：承接参数回填、确认生成等结构化回复
- `attachments`：仅在 `extract_report_template` 且需要上传原始材料时必填
- `histories`：仅在“基于历史对话生成报告”场景下必填
- `question` / `reply`：二者至少出现一个；自由提问通常使用 `question`，结构化回填通常使用 `reply`

### 3.2 Reply

```json
{
  "type": "confirm_params",
  "parameters": {
    "report_date": [
      {
        "display": "2026-04-18",
        "value": "2026-04-18",
        "query": "2026-04-18"
      }
    ]
  },
  "reportContext": {
    "templateInstance": {}
  }
}
```

规则：

- `fill_params` 用于参数补齐
- `confirm_params` 用于确认生成
- 诉求确认时必须携带完整 `templateInstance`，以支持后台做最终骨架判定和生成输入冻结
- `reply.parameters` 与 `reportContext.templateInstance.parameterValues` 都表示“已生效值”；若用户未显式填写，后台先按默认值补齐再回写
- `fill_params` 允许只提交本轮新增或修改的参数子集
- `confirm_params` 必须提交完整“已生效值”参数集；若同时带 `templateInstance.parameterValues`，两者必须同构且一致

## 4. `/chat` 响应契约

### 4.1 ChatResponse

```json
{
  "conversationId": "conv_001",
  "chatId": "chat_001",
  "status": "waiting_user",
  "steps": [],
  "ask": {},
  "answer": null,
  "errors": [],
  "requestId": "req_001",
  "timestamp": 1713427200000,
  "apiVersion": "v1"
}
```

### 4.2 Ask

```json
{
  "mode": "form",
  "type": "confirm_params",
  "title": "请确认报告诉求",
  "text": "请确认报告诉求后开始生成。",
  "parameters": [
    {
      "parameter": {
        "id": "scope",
        "label": "分析对象",
        "inputType": "dynamic",
        "required": true,
        "multi": true,
        "interactionMode": "natural_language",
        "valueMode": "query",
        "openSource": {
          "url": "https://example.internal/api/network/scopes/options"
        }
      },
      "currentValue": []
    }
  ],
  "reportContext": {
    "templateInstance": {}
  }
}
```

规则：

- `mode` 正式枚举为 `form | natural_language`
- 模板选择通过参数实现，不再单独设计模板选择阶段协议
- 诉求确认通过 `reportContext.templateInstance` 返回完整模板实例
- `Ask.parameters[*].parameter` 不是极简引用，而是模板参数定义的可渲染投影视图；字段契约应与 `ReportTemplate.parameters[*]` 保持兼容
- 多值参数的 `currentValue` 继续是三元组数组；界面可按 `display` 渲染，执行层不要直接消费拼接后的展示文本

### 4.3 Answer

```json
{
  "answerType": "REPORT",
  "answer": {
    "reportId": "rpt_001",
    "status": "generating",
    "report": {},
    "templateInstance": {},
    "documents": [],
    "generationProgress": {
      "totalSections": 8,
      "completedSections": 2
    }
  }
}
```

规则：

- `/chat` 在确认参数后返回的是“生成中的报告”
- `report` 是正式 `Report DSL`
- `templateInstance` 一并返回，支撑后续二次编辑后重新生成

### 4.4 ChatStreamEvent

```json
{
  "conversationId": "conv_001",
  "chatId": "chat_003",
  "eventType": "answer",
  "sequence": 6,
  "timestamp": 1713427200300,
  "answer": {
    "answerType": "REPORT",
    "answer": {
      "reportId": "rpt_001",
      "status": "generating",
      "report": {},
      "templateInstance": {},
      "documents": []
    }
  }
}
```

事件类型：

- `status`
- `step_delta`
- `ask`
- `answer`
- `error`
- `done`

## 5. `/reports` 契约

### 5.0 模板列表与模板详情

- 模板列表返回 `TemplateSummary`
- 模板详情返回完整 `ReportTemplate`
- `TemplateSummary` 只保留列表筛选、排序和卡片展示所需字段，不重复承载 `parameters`、`catalogs`

### 5.1 获取报告详情

```json
{
  "reportId": "rpt_001",
  "status": "available",
  "answerType": "REPORT",
  "answer": {
    "reportId": "rpt_001",
    "status": "available",
    "report": {},
    "templateInstance": {},
    "documents": []
  }
}
```

规则：

- 这是一次性返回的完成态报告
- `answer` 必须与 `/chat` 中 `REPORT.answer.answer` 结构等价
- 与流式生成时的差异只在于：这里是完整完成态
- 接口层 `status` 使用资源状态：`generating | available | failed`；不要和 `Report DSL.basicInfo.status` 的 `Running | Success | Aborted | Failed` 混用
- 顶层 `reportId/status` 必须与 `answer.reportId/status` 保持一致，不能出现资源包络和报告载荷状态不一致的情况
- 资源层与 DSL 层状态建议遵循固定映射：`generating -> (draft, Running)`、`available -> (published, Success/Aborted)`、`failed -> (*, Failed)`

### 5.2 文档生成

请求：

```json
{
  "formats": ["word", "ppt", "pdf"],
  "pdfSource": "word",
  "theme": "default",
  "strictValidation": true,
  "regenerateIfExists": false
}
```

响应：

```json
{
  "reportId": "rpt_001",
  "jobs": [
    {
      "jobId": "job_word_001",
      "format": "word",
      "status": "queued"
    },
    {
      "jobId": "job_pdf_001",
      "format": "pdf",
      "status": "blocked_by_dependency",
      "dependsOn": "job_word_001"
    }
  ],
  "documents": []
}
```

规则：

- 响应同时返回新建或复用后的 `jobs + documents`
- 当 `regenerateIfExists = false` 且已有产物可复用时，`documents` 中直接返回当前可用文档
- `documents` 的对象结构与 `ReportAnswer.documents` 保持一致

## 6. `/parameter-options/resolve` 契约

请求：

```json
{
  "parameterId": "scope",
  "openSource": {
    "url": "https://example.internal/api/network/scopes/options"
  },
  "contextValues": {
    "report_date": [
      {
        "display": "2026-04-18",
        "value": "2026-04-18",
        "query": "2026-04-18"
      }
    ]
  }
}
```

响应：

```json
{
  "options": [],
  "defaultValue": []
}
```

规则：

- 服务端用 `contextValues` 组装对外部 `openSource.url` 的统一请求体
- 外部接口的正式请求、响应结构由本设计包的 JSON Schema 统一定义
